<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.omp</groupId>
    <artifactId>centre-controle-omp</artifactId>
    <version>1.0.0-SNAPSHOT</version>
  </parent>

  <artifactId>omp-api</artifactId>
  <packaging>war</packaging>

  <name>omp-api</name>
  <description>API REST JAX-RS (/api), WebSocket JSR 356, securite JWT, migrations Flyway au demarrage. Assemble dans l'EAR aux cotes de omp-web.</description>

  <dependencies>
    <!-- Modules metier - deja presents dans l'EAR, scope provided (cf decision d'architecture #1 du plan) -->
    <dependency>
      <groupId>com.omp</groupId>
      <artifactId>omp-common</artifactId>
      <version>${project.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>com.omp</groupId>
      <artifactId>omp-port</artifactId>
      <version>${project.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>com.omp</groupId>
      <artifactId>omp-rail</artifactId>
      <version>${project.version}</version>
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>jakarta.platform</groupId>
      <artifactId>jakarta.jakartaee-api</artifactId>
      <scope>provided</scope>
    </dependency>

    <!-- Driver MySQL installe comme module WildFly cote serveur -->
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <scope>provided</scope>
    </dependency>

    <!-- Flyway - execute par FlywayMigrationStartup (@Singleton @Startup), confine a ce module -->
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-mysql</artifactId>
    </dependency>

    <!-- JWT - confine a l'API, omp-web utilise la session container-managed -->
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-api</artifactId>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-impl</artifactId>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-jackson</artifactId>
    </dependency>

    <!-- Rapports (Phase 6) : ReportService vit dans omp-common (cf son pom.xml) - ReportResource
         ne manipule que des byte[], aucune dependance POI/PDFBox directe necessaire ici. -->

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.wildfly.arquillian</groupId>
      <artifactId>wildfly-arquillian-container-managed</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>omp-api</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-war-plugin</artifactId>
      </plugin>
      <plugin>
        <!-- *ArquillianIT.java exclu du build par defaut : ses dependances (arquillian-junit5,
             shrinkwrap-resolver) ne sont ajoutees que par le profil "arquillian" ci-dessous, donc
             meme la COMPILATION (pas seulement l'execution) doit etre exclue par defaut. -->
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <testExcludes>
            <testExclude>**/*ArquillianIT.java</testExclude>
          </testExcludes>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!-- `mvn verify -Parquillian` UNIQUEMENT (jamais dans le build par defaut) : exige JBOSS_HOME
         pointant vers une distribution WildFly reelle deja installee (CI ou poste de dev), cf
         src/test/resources/arquillian.xml. Isole dans un profil pour que `mvn test`/`mvn verify`
         standard restent utilisables sans WildFly installe localement. -->
    <profile>
      <id>arquillian</id>
      <dependencies>
        <dependency>
          <groupId>org.jboss.arquillian.junit5</groupId>
          <artifactId>arquillian-junit5-container</artifactId>
          <scope>test</scope>
        </dependency>
        <dependency>
          <groupId>org.jboss.shrinkwrap.resolver</groupId>
          <artifactId>shrinkwrap-resolver-depchain</artifactId>
          <type>pom</type>
          <scope>test</scope>
        </dependency>
      </dependencies>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-compiler-plugin</artifactId>
            <configuration>
              <testExcludes combine.self="override"/>
            </configuration>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-failsafe-plugin</artifactId>
            <executions>
              <execution>
                <goals>
                  <goal>integration-test</goal>
                  <goal>verify</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

</project>
