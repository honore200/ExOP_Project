package com.omp.common.enums;

/** Valeurs bornees et stables (contrairement aux *_type pilotes par referentiel DB). */
public enum LocationType {
    STATION,
    TRACK,
    QUAY,
    ZONE,
    STORAGE
}
